import { NextConfig } from 'next';


/** @type {import('next').NextConfig} */

const nextConfig: NextConfig = {
  images: {
      domains: ['imgs.search.brave.com', 'assets.aceternity.com', 'hebbkx1anhila5yf.public.blob.vercel-storage.com', 'images.unsplash.com', 'images.pexels.com' , 'images.ctfassets.net', 'example.com', 'plus.unsplash.com', 'media.istockphoto.com', 'firebasestorage.googleapis.com', ],
  },
};

export default nextConfig;
  